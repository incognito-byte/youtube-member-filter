{
  "name": "youtube-member-filter",
  "version": "1.0.1",
  "description": "A Chrome extension for filtering YouTube members",
  "scripts": {
    "build": "webpack --mode production",
    "dev": "webpack --mode development --watch",
    "clean": "rm -rf dist",
    "package": "npm run build && cd dist && zip -r ../youtube-member-filter.zip * && cd .. && echo '✅ Extension packaged as youtube-member-filter.zip'"
  },
  "keywords": [
    "chrome-extension",
    "youtube",
    "typescript"
  ],
  "author": "incognito-byte",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "copy-webpack-plugin": "^12.0.2",
    "ts-loader": "^9.5.1",
    "typescript": "^5.5.4",
    "webpack": "^5.94.0",
    "webpack-cli": "^5.1.4"
  }
}
